Encontré el problema. Son **dos cosas** que voy a corregir juntas:

**Problema 1**: La versión `v2.6.1` es antigua, usa Node 20 (deprecado) y espera el token como parámetro `with`, no como `env`.

**Problema 2**: El `PERSONAL_ACCESS_TOKEN` debe pasarse como `personal-access-token` dentro del `with:`, no como variable de entorno.

Corrijo: